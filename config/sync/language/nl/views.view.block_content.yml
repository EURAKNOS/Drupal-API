display:
  default:
    display_options:
      pager:
        options:
          expose:
            items_per_page_label: 'Items per pagina'
      fields:
        operations:
          label: Bewerkingen
